# AES41-r-AJM-20110325.pdf

- **Source File**: `AES41-r-AJM-20110325.pdf`
- **File Size**: 237075 bytes (231.52 KB)
- **Format**: .PDF

---

## Summary & Content Preview

```text
AES 41

Abstract

AES41 provides for the carriage of audio meta-data by embedding it in the audio samples

themselves. This tightly associates the meta-data with the audio, yet makes it fragile so that

changes to the audio will invalidate the meta-data. Several meta-data sets have been defined,

covering applications such as cascaded compression (bit rate reduction), and loudness control.

This Part specifies the method by which a meta-data set is embedded in the audio samples. Other

parts define the content and format of the meta-data sets.

PART 1

0 Introduction

0.1 Rationale for this standard

It has become apparent that there are requirements for the transport of audio meta-data together

with the related audio, in a way such that the two are not separated. Further, there are requirements

where any alteration of the audio should render the related meta-data invalid. Meta-data held in

files, or transported in associated video signals, or in AES3 user bits, for example, does not meet

these requirements: The meta-data may become dissociated from the audio, or may continue to be

associated with the audio even after substantial changes have been made.

Initially, this standard was developed to satisfy these requirements, of tight association and fragility,

for an application in cascaded digital compression (bit rate reduction).

Other applications have been identified that have these same two requirements, including the

carriage of meta-data to describe loudness.

3.2 Symbols

The mathematical operators used in this standard are similar to those used in the C programming

language. The bitwise operators are defined assuming 2’s-complement representation of integers.

Numbering and counting loops generally begin from zero.

3.2.1 Arithmetic operators

3.2.1.1

+

addition

3.2.1.2

++

increment

3.2.1.3

-decrement

3.2.1.4

*

multiplication

3.2.1.5

%

modulus operator, defined only for positive numbers

3.2.2 Relational operators

3.2.2.1

<

less than

3.2.2.2

==

equal to

3.2.2.3

!=

not equal to

3.2.3 Bitwise operators

&

AND

3.2.4 Assignment

=

assignment operator

3.3 Abbreviations

The following mnemonics are defined to describe the different data types used in the coded bit

stream.

3.3.1 bslbf

bit string, left bit first, where "left" is the order in which bit strings are written in ISO/IEC 13818

NOTE Bit strings are written as a string of logic 1 s and logic 0 s within single quote marks, for

example, 1000 0001 . Blanks within a bit string are for ease of reading and have no

significance.

3.3.2 rpchof

remainder polynomial coefficients, highest order first

3.3.3 simsbf

signed integer, most significant bit first

3.3.4 uimsbf

unsigned integer, most significant bit first

7 Semantics of the bit-stream syntax [remove “coder control” wording]

The data_frame syntax shall be as described in this clause.

header

Part of the bit stream containing synchronization and content indicators.

data

Part of the bit stream containing information about the audio signal (for example,

previous encoding, loudness information).

error_check Part of the bit stream containing information for error detection.

ancillary_data Part of the bit stream that may be used for ancillary data.

7.1 Header

The first 54 bits constitute the header information which is common to all audio coder control data

types, where:

synchronization_word is the bit string ‘0000 0000 0000 0000 0000 0001 1111 1011’;

length is the number of bytes (octets) in the recoding_data_frame following the length count, up to

and including the last bit of the CRC, whereby padding_bits in the data ensure that the length is a

multiple of 8 bits;

type indicates the type of data that follow. See Table 1;

flags are bits indicating the validity of fields in the audio coder control data frame header;

introduced_time_offset_flag

is a 1-bit flag, which when set to logic 1 indicates that the

introduced time offset field in the header is valid; when s
```

## Full Extracted Text

AES 41
Abstract
AES41 provides for the carriage of audio meta-data by embedding it in the audio samples
themselves. This tightly associates the meta-data with the audio, yet makes it fragile so that
changes to the audio will invalidate the meta-data. Several meta-data sets have been defined,
covering applications such as cascaded compression (bit rate reduction), and loudness control.
This Part specifies the method by which a meta-data set is embedded in the audio samples. Other
parts define the content and format of the meta-data sets.

PART 1
0 Introduction
0.1 Rationale for this standard
It has become apparent that there are requirements for the transport of audio meta-data together
with the related audio, in a way such that the two are not separated. Further, there are requirements
where any alteration of the audio should render the related meta-data invalid. Meta-data held in
files, or transported in associated video signals, or in AES3 user bits, for example, does not meet
these requirements: The meta-data may become dissociated from the audio, or may continue to be
associated with the audio even after substantial changes have been made.
Initially, this standard was developed to satisfy these requirements, of tight association and fragility,
for an application in cascaded digital compression (bit rate reduction).
Other applications have been identified that have these same two requirements, including the
carriage of meta-data to describe loudness.

3.2 Symbols
The mathematical operators used in this standard are similar to those used in the C programming
language. The bitwise operators are defined assuming 2’s-complement representation of integers.
Numbering and counting loops generally begin from zero.

3.2.1 Arithmetic operators
3.2.1.1
+
addition
3.2.1.2
++
increment
3.2.1.3
-decrement

3.2.1.4
*
multiplication
3.2.1.5
%
modulus operator, defined only for positive numbers

3.2.2 Relational operators
3.2.2.1
<
less than
3.2.2.2
==
equal to
3.2.2.3
!=
not equal to

3.2.3 Bitwise operators
&
AND

3.2.4 Assignment
=
assignment operator

3.3 Abbreviations
The following mnemonics are defined to describe the different data types used in the coded bit
stream.
3.3.1 bslbf
bit string, left bit first, where "left" is the order in which bit strings are written in ISO/IEC 13818
NOTE Bit strings are written as a string of logic 1 s and logic 0 s within single quote marks, for
example, 1000 0001 . Blanks within a bit string are for ease of reading and have no
significance.
3.3.2 rpchof
remainder polynomial coefficients, highest order first
3.3.3 simsbf
signed integer, most significant bit first

3.3.4 uimsbf
unsigned integer, most significant bit first

7 Semantics of the bit-stream syntax [remove “coder control” wording]
The data_frame syntax shall be as described in this clause.
header

Part of the bit stream containing synchronization and content indicators.

data
Part of the bit stream containing information about the audio signal (for example,
previous encoding, loudness information).
error_check Part of the bit stream containing information for error detection.
ancillary_data Part of the bit stream that may be used for ancillary data.
7.1 Header
The first 54 bits constitute the header information which is common to all audio coder control data
types, where:
synchronization_word is the bit string ‘0000 0000 0000 0000 0000 0001 1111 1011’;
length is the number of bytes (octets) in the recoding_data_frame following the length count, up to
and including the last bit of the CRC, whereby padding_bits in the data ensure that the length is a
multiple of 8 bits;
type indicates the type of data that follow. See Table 1;
flags are bits indicating the validity of fields in the audio coder control data frame header;
introduced_time_offset_flag
is a 1-bit flag, which when set to logic 1 indicates that the
introduced time offset field in the header is valid; when set to logic 0 it indicates that the
introduced time offset field is not valid (in which case the introduced_time_offset should be set to
logic 0);
introduced_time_offset
indicates a time offset, measured in sample periods, by which
the audio signal has been shifted with respect to a reference time. A positive offset represents a
delay, a negative offset indicates that the audio signal is in advance of the reference time. The time
offset is represented by a signed integer with the most significant bit inverted to reduce the
probability of imitation of the synchronization word.
Table 1 - Meta-data types
Type
Meta data
0

Undefined (see note)

1-29

Defined by other parts of this standard, or reserved

30

Reserved

31

Reserved

NOTE The type logic 0 (undefined) should be used in situations where there is no knowledge
of a previous encoding scheme, including those where the audio has not yet been encoded, or this
transport mechanism is being used to carry data that is not one of the standardised types.

7.3 Syntax for error_check
Where:
crc_check
is a 16-bit cyclic redundancy check word used for optional error detection within the
audio coder control data stream.
This field is byte-aligned with respect to the start of the header. The bits included in the CRC check
are all those in the header and data sections. The generator polynomial for this check word is:
G(X) = X16 + X15 + X2 + 1
The method is depicted in Figure 1. The initial state of the shift register T should be ‘1111 1111
1111 1111’. All the bits to be included in the CRC are shifted into the shift register in the order in
which they occur in the audio coder control data bit stream. After the last shift operation, the
outputs b15...b0 constitute the CRC word to be used.
Data bits
T15 b15 T14 b14 T13 b13 T2 b2 T1 b1
Figure 1 — CRC check generator
7.4 Syntax for ancillary_data
Where:
ancillary_bit
carries optional information conveyed according to a user-defined format
The number of ancillary bits equals the total number of bits that can be transmitted during one audio
frame minus the number of bits in the data_frame.
The minimum data_frame length is the length of the header plus the length of the
error_check. This would be the case when the meta-data type is set to logic 0 (undefined).

8 Method of signaling the meta-data bit stream on the AES3 interface
Three signaling methods are defined for use with the AES3 interface, according to the resolution
(bits/sample) of the systems that are to be interconnected. The methods signal the data bit stream by
altering the parity of the audio samples. Even parity indicates a data bit with value logic 0, odd
parity indicates a value of logic 1.
If an audio channel can only transport the sixteen most significant bits of the AES3 sample value,
then the data should be carried by having the sixteenth most significant bit set so that the parity of
the sixteen most significant bits is as required.
If the audio channel can transport twenty or more bits transparently, then the data should be
signaled by having the twentieth most significant bit set so that the parity of the twenty most
significant bits is as required.
If the audio channel can transport twenty four or more bits transparently, then the data should be
signaled by having the twenty-fourth most significant bit set so that the parity of the twenty-four
most significant bits is as required.
If a monophonic audio signal is being carried on one channel of an AES3 interface, the data should

be signaled in the same channel as the audio signal is being carried.
If an audio signal consisting of two channels (either stereophonic or independent) is being
transmitted on an AES3 interface, separate data should be signaled for the two channels. The data
for each channel should be sent in the channel in which the signal to which it relates is being
transmitted.
Channel status data shall indicate that the interface is being used for audio.

________________________
___________________________________________________
________________________

AES 41
PART 2 MPEG-1 and MPEG-2 LSF Layer II meta-data
0 Introduction
AES41 provides for the carriage of audio meta-data by embedding it in the audio samples
themselves. This tightly associates the meta-data with the audio, yet makes it fragile so that
changes to the audio will invalidate the meta-data. Several meta-data sets have been defined,
covering applications such as cascaded compression (bit rate reduction), and loudness control.
This part describes the format for the data to be transmitted with audio decoded from MPEG-1
Layer II or MPEG-2 LSF Layer II encoding. The data may be used to aid a subsequent re-encoding
process. The presence of this data in the stream is indicated by type, as defined in Part 1 of this
standard, equal to 2.

0.1 Rationale for part 2 of this standard.
Digital compression techniques allow the use of lower bandwidth channels and less storage space
or, conversely, allow faster transmission and the storage of more program material. Digital
compression will be used for a significant amount of program acquisition, for program production
and storage, and for distribution and broadcasting.
Compression systems add impairments to sound. The audibility of these impairments depends on
the degree of compression and the techniques used.
It has been proved that re-encoding with minimal impairment can be achieved by using information
describing the previous encoding process. The information that is preserved varies according to the
encoding scheme that has been used, but might include framing and quantization resolution data.

3[should be 2] Definitions, symbols, and abbreviations
3.1 Definitions
3.1.1 analysis filter bank
filter bank in the encoder that transforms a broadband pulse-code-modulation (PCM) audio signal
into a set of subsampled sub-band samples
3.1.2 channel
means of carrying an audio signal (for example, a sequence of data forming part of a bit stream)
3.1.3 coded-audio bit stream
coded representation of an audio signal
3.1.4 compression
reduction in the number of bits used to represent an item of data
3.1.5 CRC
cyclic redundancy check
3.1.6 dual-channel mode
mode where two audio channels with independent program contents (for example, bilingual) are
encoded within one bit stream

NOTE The coding process is the same as for the stereo mode.
3.1.7 emphasis
filtering applied to an audio signal before storage or transmission (for example, to improve the
signal-to-noise ratio at high frequencies)
3.1.8 joint stereo coding
any method that exploits stereophonic irrelevance or stereophonic redundancy
3.1.9 joint stereo mode
mode of the audio coding algorithm using joint stereo coding
3.1.10 layer
one of the levels in the coding hierarchy of the audio system defined in ISO/IEC 11172-3
3.1.11 multichannel
combination of audio channels conveying the audio signals used to create a spatial sound field
3.1.12 nextbits function
nextbits()
function that permits comparison of a bit string with the next bits to be decoded in the bit stream
3.1.13 scale factor
factor by which a set of values is scaled before quantization
3.1.14 scale-factor index
numerical code for a scale factor
3.1.15 stereo mode
mode where two audio channels which form a stereophonic pair (left and right) are encoded within
one bit stream
NOTE The coding process is the same as for the dual-channel mode.
3.1.16 sub-band
subdivision of the audio frequency band
3.1.17 sub-band filter bank
set of band filters covering the entire audio frequency range
NOTE In ISO/IEC 11172-3, the sub-band filter bank is a polyphase filter bank.
3.1.18 sub-band samples
filtered samples created by the sub-band filter bank within the audio encoder, constituting a filtered
and subsampled representation of the input audio stream
NOTE From 384 time-consecutive input audio samples, twelve time-consecutive sub-band
samples are generated within each of the 32 sub-bands.
3.1.19 synthesis filter bank
filter bank in the decoder that reconstructs a PCM audio signal from sub-band samples

3 Information for transparency
In order to re-encode decoded audio with minimal impairment, it is necessary to re-encode using the
same coding decisions that were used by the previous generation (upstream) encoder. For example,
for near- transparent cascaded coding of MPEG-1 Layer II or MPEG-2 Layer II audio, the
information needed by the downstream encoder includes:
. the positions of the frame boundaries;
. the bit rate of the compressed bit stream before decoding;
. the coding mode (monophonic, joint stereo, and so on);
. the bit allocation for each sub-band within a frame;
. the scale factors for each sub-band within each sub-block within a frame.
When editing decoded audio, improved quality can be obtained if additional data are sent to
describe any timing offset that could have been introduced by editing only at points corresponding

to block boundaries (for example, at 48 kHz, 24-ms boundaries for MPEG-1 Layer II or MPEG-2
Layer II). This is particularly relevant for editing sound that has a close temporal relationship to
accompanying pictures when loss of lip-movement synchronization is a problem.
This data signal can be used by a downstream encoder to guide its coding decisions. If the audio
signal is changed in any way (for example, by mixing with another signal), then, in general, it
would not be appropriate for the downstream encoder to re-use any previous coding decisions.
However, in this case, if a suitable transport mechanism is used, the coding decision data become
corrupted and the coder is able to detect that the data are no longer valid.
In this standard, it is assumed that audio coder control data are added to the least significant bit of
the PCM audio signal in systems that handle the PCM audio with one of three different widths of
PCM sample, namely, 16-bit, 20-bit, and 24-bit samples. In order to reduce the possibility of
audibility of the coder-control data, the data are signaled by adjusting the parity of each 16-bit (20or 24-bit) word appropriately. A CRC word is appended to frames of data to provide a means to
detect errors in the data.
If the audio signal is changed in any way (for example, by mixing with another signal), then it is not
appropriate that the downstream encoder re-use any previous coding decisions. However, in this
case, the coder control data become corrupted and the coder is able to detect that the data are no
longer valid.

4 Recoding_data for MPEG-1, or MPEG-2 low sampling frequency
The data are for MPEG-1 Layer II or for MPEG-2 LSF Layer II, where:
ID is the algorithm ID, according to ISO/IEC 11172-3, that is set to logic 1 to
indicate ISO/IEC 11172-3, and set to logic 0 to indicate extension to lower
sampling frequencies specified in ISO/IEC 13818-3;
bitrate_index indicates the bit rate, according to ISO/IEC 11172-3;
sampling_frequency indicates the sampling frequency, according to ISO/IEC 11172-3;
mode indicates the mode (monophonic, stereo, joint stereo, and so on), according to
ISO/IEC 11172-3;
mode_ext
